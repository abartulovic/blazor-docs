import CodePreviewSplit from '../../components/CodePreviewSplit'
import { LessonHero, InfoBox, StepGrid, StepCard, ConceptGrid, ConceptCard, Checklist, LessonSummary } from '../../components/LessonUI'

<LessonHero
  eyebrow="AzilEdu • Predavanje 4"
  title="Baza podataka, EF Core, DbContext i prve migracije"
  subtitle="U ovoj lekciji nadograđujemo isti AzilEdu projekt tako da podaci više nisu samo ručno upisana lokalna lista, nego prelaze u pravu SQLite bazu. Fokus nije na slijepom tipkanju naredbi, nego na razumijevanju kako model, DbContext, migracija i stvarna baza rade zajedno kroz rad u Visual Studio okruženju i Package Manager Console."
  badges={['Blazor Web App', 'Entity Framework Core', 'SQLite', 'DbContext', 'Migracije']}
/>

<InfoBox type="info" title="Gdje se ova lekcija nadovezuje?">
U prethodnoj lekciji složili smo stranicu <strong>Animals</strong> i prikazivali podatke pomoću lokalne liste. To je bio dobar korak za razumijevanje modela, Razor stranice i MudTable prikaza. Sada isti projekt ide dalje: podatke više ne držimo samo u kodu, nego ih pripremamo za stvarnu pohranu u bazu.
</InfoBox>

## 1. Zašto bazu uvodimo tek sada

Da smo odmah na početku krenuli s bazom, migracijama i `DbContext` klasom, lako bismo završili tako da samo pratimo upute bez stvarnog razumijevanja projekta.

Prvo je trebalo složiti:

- strukturu solutiona
- osnovni layout i navigaciju
- model `Animal`
- stranicu `Animals`
- prikaz podataka bez baze

Tek sada ima smisla prijeći na bazu, jer točno vidimo što pokušavaš spremiti i zašto ti baza uopće treba.

<InfoBox type="success" title="Ispravan redoslijed rada">
Prvo smo razumjeli kako izgleda modul i kako se podaci prikazuju. Tek nakon toga uvodimo trajnu pohranu. Tako baza nije apstraktna teorija nego logičan nastavak onoga što smo već napravili.
</InfoBox>

## 2. Što danas radiš

<StepGrid>
  <StepCard title="1. Provjeravaš model Animal" icon="pets">
    Krećemo od istog modela koji smo koristili u prethodnoj lekciji i usklađujemo ga s bazom.
  </StepCard>
  <StepCard title="2. Dodaješ EF Core pakete" icon="package">
    U projekt `AzilEdu.Api` dodajemo NuGet pakete potrebne za SQLite i migracije.
  </StepCard>
  <StepCard title="3. Radiš DbContext" icon="database">
    Uvodi se klasa koja predstavlja vezu između aplikacije i baze.
  </StepCard>
  <StepCard title="4. Registriraš bazu" icon="link">
    U `Program.cs` povezujemo `DbContext` i connection string.
  </StepCard>
  <StepCard title="5. Pokrećeš migracije" icon="wand">
    Generiramo prvu migraciju i stvaramo stvarnu SQLite bazu.
  </StepCard>
</StepGrid>

## 3. Podsjetnik na raspodjelu projekata

<ConceptGrid>
  <ConceptCard title="AzilEdu.App">
    Ovdje se nalazi korisničko sučelje: Razor stranice, MudBlazor komponente, navigacija i prikaz podataka.
  </ConceptCard>
  <ConceptCard title="AzilEdu.Api">
    Ovdje smještaš bazu, `DbContext`, API logiku, kontrolere i migracije.
  </ConceptCard>
  <ConceptCard title="AzilEdu.Shared">
    Ovdje držiš zajedničke modele koje koriste i frontend i backend.
  </ConceptCard>
</ConceptGrid>

<InfoBox type="warning" title="Važna odluka za ovu lekciju">
SQLite bazu i `DbContext` postavljaš u projektu <strong>`AzilEdu.Api`</strong>. To je ispravno jer backend upravlja podacima i pristupom bazi. Frontend projekt ne bi trebao direktno upravljati bazom.
</InfoBox>

## 4. Provjera modela `Animal`

Prije baze moramo imati model koji želimo spremati. Ovdje se ne vraćamo na siromašniju staru verziju modela, nego koristimo usklađenu verziju iz prethodne lekcije.

Najčešća lokacija modela je:

```text
Shared/Models/Animal.cs
```

<CodePreviewSplit
  title='Lokacija datoteke `Animal.cs`'
  description='Na lijevoj strani vidimo putanju datoteke koju uređuješ, a na desnoj preview slike iz Visual Studija.'
  language="text"
  fileName="animal-model-location.webp"
  code={`Shared/Models/Animal.cs`}
/>

Opis slike: prikaz foldera `Shared/Models` i otvorene datoteke `Animal.cs` u Visual Studio okruženju.

### Cjeloviti sadržaj datoteke `Animal.cs`

```csharp
namespace AzilEdu.Shared.Models;

public class Animal
{
    public int Id { get; set; }
    public string Name { get; set; } = string.Empty;
    public string Species { get; set; } = string.Empty;
    public string Breed { get; set; } = string.Empty;
    public string Gender { get; set; } = string.Empty;
    public int Age { get; set; }
    public DateTime ArrivalDate { get; set; }
    public bool IsAdopted { get; set; }
    public string ImageUrl { get; set; } = string.Empty;
    public string Description { get; set; } = string.Empty;
}
```

### Zašto je ovaj model važan za bazu

Svako javno svojstvo iz ove klase EF Core može pretvoriti u stupac tablice. To znači:

- `Id` postaje primarni ključ
- `Name`, `Species`, `Breed` i ostala tekstualna svojstva postaju stupci za tekst
- `Age` postaje numerički stupac
- `ArrivalDate` postaje datum
- `IsAdopted` postaje logička vrijednost

<InfoBox type="info" title="Najvažnija mentalna poveznica">
Klasa `Animal` još nije baza. To je model u aplikaciji. Tek kada taj model povežeš s `DbContext` klasom i pokreneš migraciju, EF Core može iz njega izvesti strukturu tablice u bazi.
</InfoBox>

## 5. Dodavanje EF Core paketa u `AzilEdu.Api`

Entity Framework Core ne dolazi spreman samo zato što koristimo .NET projekt. Moraš dodati pakete koji omogućuju rad s bazom i migracijama.

Potrebni su ti:

- `Microsoft.EntityFrameworkCore`
- `Microsoft.EntityFrameworkCore.Sqlite`
- `Microsoft.EntityFrameworkCore.Tools`

### Kako otvoriti Package Manager Console u Visual Studiju

U Visual Studiju otvori:

```text
Tools > NuGet Package Manager > Package Manager Console
```

Kada se konzola otvori, na vrhu provjeri da je **Default project** postavljen na `AzilEdu.Api`.

### Naredbe koje pokrećemo u Package Manager Console

U Package Manager Console upiši:

```powershell
Install-Package Microsoft.EntityFrameworkCore
Install-Package Microsoft.EntityFrameworkCore.Sqlite
Install-Package Microsoft.EntityFrameworkCore.Tools
```

<CodePreviewSplit
  title='Provjera paketa u `AzilEdu.Api`'
  description='Na lijevoj strani vidimo gdje u Solution Exploreru ili Dependencies prikazu trebamo provjeriti instalirane pakete, a na desnoj preview slike.'
  language="text"
  fileName="api-nuget-packages.webp"
  code={`AzilEdu.Api
└── Dependencies
    └── Packages
        ├── Microsoft.EntityFrameworkCore
        ├── Microsoft.EntityFrameworkCore.Sqlite
        └── Microsoft.EntityFrameworkCore.Tools`}
/>

Opis slike: otvoren `AzilEdu.Api` projekt i prikaz `Dependencies/Packages` s instaliranim EF Core paketima.

<InfoBox type="warning" title="Tipična pogreška">
Nemoj pakete dodavati u `AzilEdu.App`. Za ovu lekciju paketi moraju biti u <strong>`AzilEdu.Api`</strong>, jer se ondje nalaze baza, migracije i `DbContext`.
</InfoBox>

## 6. Kreiranje mape `Data` i klase `AzilEduDbContext`

Sada uvodiš novu klasu koja je središte rada s bazom.

Preporučena struktura u API projektu je:

```text
AzilEdu.Api/
├── Data/
│   └── AzilEduDbContext.cs
├── Program.cs
├── appsettings.json
└── ...
```

<CodePreviewSplit
  title='Lokacija datoteke `AzilEduDbContext.cs`'
  description='Na lijevoj strani vidimo putanju i naziv datoteke koju sada izrađujemo, a na desnoj preview slike iz Visual Studija.'
  language="text"
  fileName="dbcontext-file-location.webp"
  code={`AzilEdu.Api/Data/AzilEduDbContext.cs`}
/>

Opis slike: prikaz projekta `AzilEdu.Api`, mape `Data` i otvorene datoteke `AzilEduDbContext.cs`.

### 6.1 `using` direktive i namespace

```csharp
using AzilEdu.Shared.Models;
using Microsoft.EntityFrameworkCore;

namespace AzilEdu.Api.Data;
```

Ovaj dio omogućuje:

- korištenje modela `Animal` iz Shared projekta
- korištenje EF Core klase `DbContext`
- pravilno smještanje nove klase u namespace API projekta

### 6.2 Nasljeđivanje `DbContext` klase

```csharp
public class AzilEduDbContext : DbContext
```

Ovdje govoriš da je `AzilEduDbContext` posebna klasa za rad s bazom i da nasljeđuje sve osnovne mogućnosti koje daje EF Core.

### 6.3 Konstruktor

```csharp
public AzilEduDbContext(DbContextOptions<AzilEduDbContext> options)
    : base(options)
{
}
```

Ovaj konstruktor prima konfiguraciju baze izvana, najčešće iz `Program.cs` datoteke. To je standardan način rada u modernim .NET aplikacijama.

### 6.4 `DbSet<Animal>`

```csharp
public DbSet<Animal> Animals => Set<Animal>();
```

Ova linija govori EF Coreu da u bazi želimo imati skup zapisa tipa `Animal`.

Drugim riječima:

- model `Animal` opisuje jedan zapis
- `DbSet<Animal>` predstavlja kolekciju tih zapisa
- u bazi to najčešće postaje tablica `Animals`

### Cjeloviti sadržaj datoteke `AzilEduDbContext.cs`

```csharp
using AzilEdu.Shared.Models;
using Microsoft.EntityFrameworkCore;

namespace AzilEdu.Api.Data;

public class AzilEduDbContext : DbContext
{
    public AzilEduDbContext(DbContextOptions<AzilEduDbContext> options)
        : base(options)
    {
    }

    public DbSet<Animal> Animals => Set<Animal>();
}
```

## 7. Dodavanje connection stringa u `appsettings.json`

Aplikacija mora znati gdje se baza nalazi. To rješavaš connection stringom.

### Dio koji dodajemo

```json
"ConnectionStrings": {
  "DefaultConnection": "Data Source=AzilEdu.db"
}
```

Ovaj zapis znači da će SQLite koristiti lokalnu datoteku baze naziva `AzilEdu.db`.

### Cjeloviti primjer `appsettings.json`

```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Data Source=AzilEdu.db"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
```

<CodePreviewSplit
  title='Connection string u `appsettings.json`'
  description='Na lijevoj strani vidimo dio konfiguracije koji trebamo dodati, a na desnoj preview kako to izgleda u editoru.'
  language="json"
  fileName="appsettings-connectionstring.webp"
  code={`"ConnectionStrings": {
  "DefaultConnection": "Data Source=AzilEdu.db"
}`}
/>

Opis slike: otvorena datoteka `appsettings.json` i označen dio `ConnectionStrings` s vrijednošću `DefaultConnection`.

<InfoBox type="info" title="Gdje će se baza pojaviti?">
Za ovu lekciju najvažnije je razumjeti da je SQLite baza u ovom slučaju <strong>jedna `.db` datoteka</strong>. Ona se može pojaviti u projektu ovisno o načinu pokretanja aplikacije i konfiguraciji projekta.
</InfoBox>

## 9. Što je migracija

Migracija je zapis promjene strukture baze na temelju promjena koje si napravio u modelima i `DbContext` klasi.

Najkraće rečeno:

- imamo klasu `Animal`
- imamo `DbSet<Animal>` u `AzilEduDbContext` klasi
- EF Core iz toga zaključuje da treba napraviti tablicu
- migracija opisuje kako baza treba izgledati nakon te promjene

<InfoBox type="info" title="Definicija koju vrijedi zapamtiti">
Migracija je datoteka koja opisuje promjenu baze podataka. `Add-Migration` stvara opis promjene, a `Update-Database` tu promjenu primjenjuje na stvarnu bazu.
</InfoBox>

## 10. Prva migracija za `Animals`

Prije prve migracije moramo imati tri stvari:

- model `Animal`
- `AzilEduDbContext` s `DbSet<Animal>`
- ispravno postavljen **Default project** na `AzilEdu.Api` u Package Manager Console

### 10.1 Gdje pišemo naredbe

Sve naredbe u ovoj lekciji pišemo u **Package Manager Console** unutar Visual Studija.

Otvori je ovako:

```text
Tools > NuGet Package Manager > Package Manager Console
```

### 10.2 Prije naredbe provjeri projekt

Najvažnije je da je u Package Manager Console kao **Default project** odabran **`AzilEdu.Api`**.

<CodePreviewSplit
  title='Provjera da radiš u `AzilEdu.Api` projektu'
  description='Na lijevoj strani vidimo što treba biti odabrano prije migracije, a na desnoj preview slike iz Visual Studija.'
  language="text"
  fileName="pm-default-project-api.webp"
  code={`Package Manager Console
Default project: AzilEdu.Api`}
/>

Opis slike: otvoren Package Manager Console u Visual Studiju i označeno da je Default project postavljen na `AzilEdu.Api`.

### 10.3 Naredba za prvu migraciju

Kada je `AzilEdu.Api` odabran kao Default project, pokreni:

```powershell
Add-Migration InitialCreate
```

Ako je sve postavljeno ispravno, EF Core će stvoriti mapu `Migrations` i u nju dodati datoteke koje opisuju početnu strukturu baze.

<CodePreviewSplit
  title='Nakon `Add-Migration InitialCreate`'
  description='Na lijevoj strani vidimo što očekuješ u strukturi projekta nakon migracije, a na desnoj preview slike iz Solution Explorera.'
  language="text"
  fileName="migrations-folder-created.webp"
  code={`AzilEdu.Api/
├── Data/
├── Migrations/
│   ├── 2025xxxxxxxx_InitialCreate.cs
│   ├── 2025xxxxxxxx_InitialCreate.Designer.cs
│   └── AzilEduDbContextModelSnapshot.cs
└── ...`}
/>

Opis slike: Solution Explorer s novom mapom `Migrations` nakon uspješno izvršene naredbe `Add-Migration InitialCreate`.

## 11. Primjena migracije na stvarnu bazu

Sada imamo opis promjene, ali baza još ne mora postojati. Zato u istoj **Package Manager Console** pokrećemo naredbu:

```powershell
Update-Database
```

Ovdje se događa stvarna promjena:

- stvara se SQLite baza
- u bazi nastaje tablica `Animals`
- struktura baze sada odgovara modelu i `DbContext` klasi

<InfoBox type="success" title="Razlika koju moramo jasno razlikovati">
- `Add-Migration` stvara opis promjene
- `Update-Database` tu promjenu primjenjuje na stvarnu bazu
</InfoBox>

<CodePreviewSplit
  title='Provjera SQLite baze nakon `Update-Database`'
  description='Na lijevoj strani vidimo naziv datoteke koju tražiš, a na desnoj preview kako to može izgledati u projektu ili File Exploreru.'
  language="text"
  fileName="sqlite-db-file.webp"
  code={`AzilEdu.db`}
/>

Opis slike: prikaz datoteke `AzilEdu.db` u projektu, output folderu ili File Exploreru nakon uspješne primjene migracije.

## 12. Domaći rad: samostalno dodaj tablicu `HousingUnits`

Za domaći ne ponavljamo opet `Animals`, nego isti obrazac samostalno prenosimo na novu tablicu. Odabir je ovdje namjerno strogo zadan kako bi zadatak bio jasan.

### 12.1 Napravi model `HousingUnit`

U projektu `AzilEdu.Shared`, u mapi `Models`, dodaj novu datoteku:

```text
Shared/Models/HousingUnit.cs
```

Model treba sadržavati točno ova svojstva:

```csharp
namespace AzilEdu.Shared.Models;

public class HousingUnit
{
    public int Id { get; set; }
    public string Name { get; set; } = string.Empty;
    public string UnitType { get; set; } = string.Empty;
    public int Capacity { get; set; }
    public int CurrentOccupancy { get; set; }
    public bool IsIndoor { get; set; }
}
```

### 12.2 Nadogradi `AzilEduDbContext`

U `AzilEduDbContext.cs` dodaj još jedan `DbSet`:

```csharp
public DbSet<HousingUnit> HousingUnits => Set<HousingUnit>();
```

### 12.3 Napravi novu migraciju

Nemoj ponovno koristiti `InitialCreate`. Sada radiš novu migraciju. U Package Manager Console, uz odabran `AzilEdu.Api`, pokreni:

```powershell
Add-Migration AddHousingUnits
```

### 12.4 Primijeni promjenu na bazu

U istoj Package Manager Console pokreni:

```powershell
Update-Database
```

### 12.5 Što na kraju mora postojati

Na kraju domaćeg treba postojati:

- novu datoteku `HousingUnit.cs`
- nadograđen `AzilEduDbContext`
- novu migraciju `AddHousingUnits`
- ažuriranu SQLite bazu
- objašnjenje vlastitim riječima što se promijenilo u bazi nakon dodavanja nove tablice

### 12.6 Kako gotov rad treba izgledati

Predaja treba sadržavati:

- screenshot modela `HousingUnit`
- screenshot `AzilEduDbContext` klase s oba `DbSet` svojstva
- screenshot `Migrations` mape nakon nove migracije
- kratki tekst od 5 do 8 rečenica u kojem objašnjavaš razliku između modela, migracije i stvarne baze

<InfoBox type="warning" title="Što ovdje nije prihvatljivo">
Nije dovoljno samo kopirati naredbe. Mora biti jasno da razumiješ što je dodano, zašto je potrebna nova migracija i što se promijenilo u strukturi baze.
</InfoBox>

## 13. Najvažnije za zapamtiti

- model u aplikaciji i tablica u bazi nisu ista stvar, ali su povezani
- `DbContext` je most između aplikacije i baze
- `DbSet<Animal>` i `DbSet<HousingUnit>` govore EF Coreu koje tablice želimo imati
- `Add-Migration` ne stvara odmah bazu, nego zapisuje promjenu strukture
- `Update-Database` tu promjenu primjenjuje na stvarnu bazu
- SQLite baza je u ovom slučaju jedna `.db` datoteka
- svaka nova tablica ili veća promjena modela traži novu migraciju

<Checklist
  items={[
    'Znam objasniti razliku između modela, DbContext klase, migracije i stvarne baze.',
    'Znam gdje se dodaju EF Core SQLite paketi.',
    'Znam napraviti AzilEduDbContext u projektu AzilEdu.Api.',
    'Znam dodati connection string u appsettings.json.',
    'Znam registrirati DbContext u Program.cs datoteci.',
    'Znam razliku između Add-Migration i Update-Database naredbi.',
    'Znam samostalno dodati novu tablicu HousingUnits kroz novi model i novu migraciju.'
  ]}
/>

<LessonSummary
  title="Sažetak lekcije"
  points={[
    'U ovoj lekciji AzilEdu projekt prvi put dobiva stvarnu bazu podataka.',
    'Koristiš postojeći model Animal i povezuješ ga s EF Coreom preko klase AzilEduDbContext.',
    'Dodaješ SQLite connection string i registriraš bazu u Program.cs datoteci.',
    'Pokrećeš prvu migraciju i zatim je primjenjuješ na stvarnu bazu.',
    'Na satu prolazimo cijeli tok za Animals, a u domaćem isti princip prenosimo na novu tablicu HousingUnits.'
  ]}
/>
